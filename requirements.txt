# 平台特异依赖（torch / torchaudio / llama-cpp-python）不在此文件，
# 由 setup.bat / build_portable.py 按显卡（CUDA vs CPU）单独安装。
# 原因：torch 与 torchaudio 版本必须严格匹配，且 CPU/CUDA 走不同 index。

# -- LLM
openai>=1.0.0
sentence-transformers>=3.0.0
jinja2>=3.1.0

# -- Speech Recognition
funasr>=1.0.0
modelscope>=1.9.0

# -- Audio
sounddevice>=0.5.0
soundfile>=0.13.0
soundcard>=0.4.0
numpy>=1.26.0
scipy>=1.10.0
librosa>=0.10.0

# -- Echo Cancellation (optional, needs torch+CUDA)
DeepFilterNet>=0.5.0

# -- Speaker Identification (optional)
resemblyzer>=0.1.0

# -- Long-term Memory
chromadb>=1.0.0

# -- GUI
PyQt6>=6.0.0
PyQt6-WebEngine>=6.0.0
qasync>=0.27.0

# -- VTube Studio (optional)
pyvts>=0.3.0

# -- Utilities
PyYAML>=6.0
aiohttp>=3.8.0
keyboard>=0.13.0
psutil>=5.9.0
tenacity>=8.0.0
requests>=2.28.0
websocket-client>=1.0.0
python-dotenv>=0.19.0

# -- Web Search
ddgs>=9.0.0
trafilatura>=2.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# -- Vision
opencv-python>=4.8.0
mss>=9.0.0
